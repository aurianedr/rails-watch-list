<h1>Create a movies list </h1>

<%= simple_form_for(@list) do |f| %>
  <%= f.input :name %>
  <%= f.submit %>
<% end %>
<div class = mt-3>
  <%= link_to "Back to all lists", lists_path, :class => "btn btn-outline-secondary" %>
</div>
